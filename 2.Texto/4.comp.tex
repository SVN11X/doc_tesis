\chapter{Componentes}
\label{ch:componentes}

Este capítulo presenta los componentes principales del prototipo y los parámetros físicos que se utilizan en el modelo cinemático, el firmware y la integración con \gls{ROS2}. El robot corresponde a una plataforma diferencial de bajo costo compuesta por dos ruedas motrices, una rueda loca, motores DC con encoders, driver L298N, un \gls{Arduino} Nano para control de bajo nivel, una Raspberry Pi 4 como computador principal y un sensor RPLidar A1 para percepción del entorno.

%------------------------------------------------------------------
\section{Arquitectura general del prototipo}

El prototipo adopta una configuración de tracción diferencial, con dos ruedas motrices independientes y una rueda loca de apoyo. Esta arquitectura permite controlar el desplazamiento mediante la diferencia de velocidad entre las ruedas izquierda y derecha, lo que facilita la implementación del modelo cinemático desarrollado en el Capítulo~\ref{marco_teorico}.

\utemFigure{2.Texto/Imag/diff_bec.png}{0.38\textwidth}
{Arquitectura móvil de tracción diferencial.}
{BECChassisLong}{fig:chasis-2wd-caster}

La Tabla~\ref{tab:componentes_principales} resume los componentes empleados y su función dentro del sistema.

\begin{table}[H]
\centering
\caption{Componentes principales del prototipo.}
\label{tab:componentes_principales}
\small
\begin{tabular}{p{3.2cm} p{4.5cm} p{5.2cm}}
\toprule
\textbf{Subsistema} & \textbf{Componente} & \textbf{Función principal} \\
\midrule
Locomoción & Motores DC JGA25-370 con ruedas de goma & Generar el movimiento diferencial del robot. \\
Odometría & Encoders de cuadratura Hall & Medir el desplazamiento de cada rueda. \\
Potencia de motores & Driver L298N & Accionar los motores mediante señales \gls{PWM}. \\
Control de bajo nivel & \gls{Arduino} Nano & Ejecutar lectura de encoders y control PID de velocidad. \\
Cómputo principal & Raspberry Pi 4 & Ejecutar \gls{ROS2}, SLAM, navegación y comunicación con periféricos. \\
Percepción & RPLidar A1 & Obtener barridos láser 2D para mapeo y evasión de obstáculos. \\
Energía & Batería Li-ion 12 V y regulador buck 5 V & Alimentar motores, lógica y sensores. \\
Estructura & Chasis impreso en 3D & Integrar mecánicamente los componentes del robot. \\
\bottomrule
\end{tabular}
\end{table}

%------------------------------------------------------------------
\section{Actuación, odometría y control de bajo nivel}

La actuación se realiza mediante dos motorreductores JGA25-370 de 12 V, seleccionados por su disponibilidad, bajo costo y torque suficiente para una plataforma diferencial de interiores. Cada motor incorpora un encoder incremental de cuadratura, utilizado para estimar velocidad y desplazamiento de rueda.

La resolución teórica del encoder en el eje de salida se obtiene a partir de la resolución del sensor Hall en el eje del motor, la decodificación en cuadratura y la relación de reducción:

\begin{equation}
N_e^{\mathrm{teo}} = N_{\mathrm{Hall}} \cdot c_q \cdot N_g
\label{eq:resolucion_encoder}
\end{equation}

donde $N_e^{\mathrm{teo}}$ es la resolución teórica del encoder [cuentas/vuelta del eje de salida], $N_{\mathrm{Hall}}=11$ pulsos por vuelta del eje del motor, $c_q=4$ el factor de decodificación en cuadratura (adimensional) y $N_g=46$ la relación de reducción nominal (adimensional, correspondiente a $46{:}1$). Evaluando, $N_e^{\mathrm{teo}}=11\cdot 4\cdot 46 = 2024$ cuentas/vuelta. La medición experimental entregó un valor promedio cercano a 1980 cuentas/vuelta, que se adopta como resolución efectiva $N_e=1980$ cuentas/vuelta, constante unificada para el firmware y la odometría.

El control de bajo nivel se ejecuta en un \gls{Arduino} Nano, encargado de leer los encoders, calcular el PID de velocidad de cada rueda y enviar señales \gls{PWM} al driver L298N. Este último permite accionar ambos motores, aunque introduce una zona muerta y caída de tensión que deben compensarse mediante la sintonización experimental del controlador.

%------------------------------------------------------------------
\section{Cómputo, percepción e integración con ROS 2}

La Raspberry Pi 4 ejecuta Ubuntu 20.04 y \gls{ROS2} Foxy, actuando como computador principal del robot. Su función es coordinar la comunicación con el \gls{Arduino}, recibir los datos del sensor láser, ejecutar \texttt{slam\_toolbox}, Nav2 y los nodos asociados a la navegación.

El sensor principal de percepción es el RPLidar A1, un escáner láser 2D de 360 grados que publica barridos utilizados por SLAM y por los mapas de costo de Nav2. Se selecciona por su bajo costo, integración con \gls{ROS2} y adecuación a entornos interiores planos. Como limitación, no detecta obstáculos fuera del plano de barrido.

%------------------------------------------------------------------
\section{Alimentación y fabricación}

El sistema se alimenta desde una batería Li-ion de 12 V. Desde este bus principal se alimentan los motores y, mediante un convertidor reductor, se obtiene el riel de 5 V para la Raspberry Pi, el \gls{Arduino} y el \gls{LIDAR}. La distribución separa potencia y lógica para reducir interferencias producidas por los motores.

El chasis y las cubiertas fueron fabricados mediante impresión 3D FDM en PLA+. El diseño considera espacio para la Raspberry Pi, el \gls{Arduino}, el driver de motores, el regulador de tensión, la batería y el sensor RPLidar A1. Las vistas adicionales, parámetros de impresión y detalles de montaje se incluyen en los anexos.

\utemFigurePropia{2.Texto/Imag/fig_prototipo_lateral_izquierda.jpg}{0.55\linewidth}
{Vista lateral del prototipo construido.}
{fig:prototipo}

%------------------------------------------------------------------
\section{Parámetros físicos del modelo}
\label{sec:parametros_modelo}

La Tabla~\ref{tab:parametros_modelo} reúne los parámetros físicos usados por el modelo cinemático, la odometría, el gemelo digital y el firmware. Estos valores constituyen la referencia común para los capítulos posteriores.

\begin{table}[H]
\centering
\caption{Parámetros físicos empleados en el modelo y el firmware.}
\label{tab:parametros_modelo}
\small
\begin{tabular}{c p{4.2cm} p{3.0cm} p{4.2cm}}
\toprule
\textbf{Símbolo} & \textbf{Parámetro} & \textbf{Valor} & \textbf{Uso principal} \\
\midrule
$r$ & Radio de rueda & 0.0335 m & Modelo cinemático y odometría \\
$C$ & Circunferencia de rueda & 0.2105 m & Conversión de cuentas a distancia \\
$L$ & Distancia entre ruedas & 0.188 m & Modelo diferencial \\
$N_g$ & Relación de reducción & 46 (nominal $46{:}1$) & Cálculo teórico del encoder \\
$N_e^{\mathrm{teo}}$ & Resolución teórica del encoder & 2024 cuentas/vuelta & Cálculo teórico \\
$N_e$ & Resolución efectiva del encoder & 1980 cuentas/vuelta & Firmware y odometría \\
$c_q$ & Factor de cuadratura & 4 & Decodificación A/B \\
$T_s$ & Período de control & $1/30$ s & PID y odometría \\
\bottomrule
\end{tabular}
\end{table}

%------------------------------------------------------------------
\section*{Conclusión del capítulo}

El prototipo se construye a partir de componentes accesibles y compatibles con una arquitectura de navegación basada en \gls{ROS2}. La plataforma diferencial, los encoders, el \gls{Arduino}, la Raspberry Pi 4 y el RPLidar A1 permiten implementar odometría, control de bajo nivel, mapeo 2D y navegación autónoma. Los parámetros físicos consolidados en este capítulo se utilizan como base para el gemelo digital, el firmware y la validación experimental.